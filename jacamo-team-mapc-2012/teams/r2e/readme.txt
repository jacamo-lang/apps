Nesse time foram implementadas as seguintes funcionalidades:
1) Mesmas idéias do r2d, mas acrescentado um comportamento para os exploradores poderem acessar vertices distantes para dar probe (longo prazo).
2) Essa exploracao eh baseada em 2 estrategias, uma delas randomica se usado !tryPathToProbe(List) e outra que busca o vertice mais proximo !tryPathToProbeClosest(List). Por padrao eh usada a ideia do vertice mais proximo.

type
    > ant
to run the team

